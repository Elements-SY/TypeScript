<!-- 

 1. 接口(本章节我们使用ts语法中的接口方式给函数的参数添加一个约束方式，
 在在这个例子里，我们希望 greeter函数接收一个字符串参数)

 2. 在当前目录下打开终端执行：tsc index.ts

 3. 当我们执行tsc index.ts时，发现index.ts文件中报红色的标注，
 警告函数实现重复。这个时候我们在当前目录打开终端执行tsc --init即可。

 4. 在当前目录下打开终端执行：tsc --init。或者 执行tsc index.ts && tsc --init
 -->